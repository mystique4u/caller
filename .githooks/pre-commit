#!/bin/bash
# Git pre-commit hook — runs all validation checks before committing
# Installed via: git config core.hooksPath .githooks
REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/scripts/pre-push-check.sh"
